# IMPLEMENT PHASE — {props.phaseName}

Phase ID: {props.phaseId}

## Objective

Implement the code for this phase according to the plan.

## Context

- Plan file: {props.planFilePath}
- Research context: {props.contextFilePath}

## Phase Description

{props.phaseDescription}

## Project-Specific Instructions

{props.projectInstructions}

## Implementation Guidelines

1. Follow the plan step by step
2. Use TypeScript with strict types
3. Use TailwindCSS for all styling (no CSS files)
4. React components should be functional with hooks
5. Keep components small and focused
6. Use meaningful names for variables and functions
7. Add JSDoc comments for complex logic
8. Commit after each logical unit is complete

## Code Style

- Prefer `const` over `let`
- Use arrow functions
- Destructure props and state
- Use template literals
- Group imports: React, external libs, local files

## REQUIRED OUTPUT

{props.schema}
